RRA NLP Engine: TPU Black Box Hyperparameter Discovery (2026-05-15)
1. Executive Summary
The RRA NLP Engine aims to bypass transformer architectures by using a biologically inspired Spiking Neural Network (SNN) with holographic VSA encoding and a TPU-discovered Universal Plasticity MLP. This document records the engineering of the evolutionary "Black Box" (`scripts/colab_tpu_nlp_blackbox.py`) that discovers the optimal biophysical constants for real-time text processing on a Google TPU v5e-1.

Current Status: **23/23 PERFECT** — `HELLO WORLD THIS IS RRA` recovered exactly at epoch 3800. The system uses a Reservoir Computing architecture: the TPU-discovered genome controls a frozen 128-neuron SNN, with a trained 256→2 linear readout achieving 100% character accuracy.

2. Architecture: MXU-Aligned SNN
The network is aligned to the TPU v5e-1's Matrix Multiply Unit (MXU), which operates on 128×128 tiles.

Layout (N=128, 1 MXU Tile):
| Pool | Neurons | Range | Function |
|---|---|---|---|
| Sensory | 32 | [0, 31] | Gaussian population code (σ=0.08) from holographic binder |
| Excitatory Hidden | 48 | [32, 79] | Recurrent processing with learned weights |
| Inhibitory Hidden | 16 | [80, 95] | Dale's Law: negative feedback, frozen weights |
| Motor | 32 | [96, 127] | Population rate coding: first 16 = X coord, last 16 = Y coord |

Key Design: A single fused `dot(spikes, W * sign[:, None])` per tick, where `sign = EXC_SRC * ampa - INH_SRC * gaba`. This computes excitatory AND inhibitory synaptic input in ONE MXU cycle with zero padding waste.

3. The Genome (8 Genes)
Each brain in the population is parameterized by 8 scalar genes:

| Gene | Symbol | Range | Description |
|---|---|---|---|
| `sensory_gain` | sg | [1, 200] | mV injected per active sensory neuron per tick |
| `binder_alpha` | ba | [0.01, 0.99] | Holographic binding momentum (context vs. token) |
| `weight_scale` | ws | [0.01, 5.0] | Initial synaptic weight magnitude |
| `ampa_jump` | aj | [0.1, 50] | Excitatory voltage jump per presynaptic spike |
| `gaba_drop` | gd | [0.1, 50] | Inhibitory voltage drop per presynaptic spike |
| `lr_scale` | lr | [0.001, 2.0] | Plasticity learning rate multiplier |
| `thresh_offset` | to | [-10, +10] | Firing threshold adjustment from -56.5 mV |
| `tau_m` | tm | [5, 100] ms | Membrane time constant (leak rate) |

4. Universal Plasticity MLP (Scalar Modulator)
The 369-parameter MLP discovered by prior TPU evolution (`weights.txt`) is used as a global dopaminergic gate:

$$\text{plasticity\_mod} = \text{MLP}(\overline{\text{trace\_pre}}, \overline{\text{trace\_post}}, \overline{\text{trace\_Ca}}, \text{reward})$$

This scalar modulates a local Hebbian outer product:
$$\Delta W = \text{plasticity\_mod} \times (\text{trace\_pre} \otimes \text{post\_proxy}) \times \text{lr\_scale}$$

Biological Analogy: The MLP acts as the Ventral Tegmental Area (VTA), broadcasting a single dopamine signal. Local synapses decide WHAT to learn (Hebbian correlation), while the VTA decides HOW MUCH to learn (reward gating).

Constraint: Only excitatory source rows (`EXC_SRC[:, None]`) are modified. Inhibitory weights are frozen per Dale's Law.

5. Fitness Function & Known Bugs

Loss Metric: $\text{loss} = \frac{1}{T} \sum_t ||\text{motor\_output}_t - \text{target\_coord}_t||^2$

This measures the Euclidean distance between the motor neuron's decoded 2D output and the target token's coordinate in the 16×16 vocabulary grid.

Homeostasis Penalty:
- Dead brain (firing rate < 10% of ideal): +50.0
- Epileptic brain (firing rate > 300% of ideal): +20.0
- Gentle quadratic pull toward 5% target rate: $(r - 1)^2 \times 2.0$

Inner-Epoch Training: Each brain is trained for 5 epochs internally. Fitness is measured on the LAST epoch only, forcing the black box to discover parameters that enable actual learning, not just static lucky states.

Bug Report — The 0.327 Floor (RESOLVED):
The loss converged to exactly 0.3270 across 80 generations (6-80) with every genome configuration.

Root Cause (Confirmed): The motor output was normalized to the unit circle:
`mout = mc / (norm(mc) + 1e-6)` — output always has length 1, living on the circle $x^2 + y^2 = 1$.
But target coordinates are in $[0, 1]^2$. The motor could only encode a DIRECTION (1D angle), not a POSITION (2D coordinate). 0.3270 is the average squared distance from the optimal unit-circle point to all target token coordinates — a mathematical ceiling, not a learning failure.

Fix Applied:
1. Population rate coding: `X = mean(motor_spikes[0:16]) / TICKS`, `Y = mean(motor_spikes[16:32]) / TICKS`. Output naturally maps to $[0, 1]^2$.
2. Dense sensory→motor shortcut: `topo[:N_S, N-N_M:] = 1.0`. Guarantees that different sensory inputs produce measurably different motor activity.

6. Experimental Results (TPU v5e-1)

Run 1 (Pre-Fix: unit-circle motor decode):
| Gen | Loss | Spikes | Notes |
|---|---|---|---|
| 1-6 | 168→0.327 | 70K→7.3K | Fast convergence to floor |
| 7-80 | 0.327 | 7,360 | **Stuck** — mathematical ceiling from normalization |

Run 2 (Post-Fix: population rate coding + dense sensory→motor shortcut):
| Gen | Loss | Spikes | Notes |
|---|---|---|---|
| 1 | 175.88 | 72,184 | Initial exploration |
| 7 | 0.330 | 7,075 | Broke through old floor! |
| 8 | 0.149 | 7,516 | Below 0.327 — fix confirmed |
| 18 | 0.098 | 7,536 | Sub-0.1 |
| 32 | 0.084 | 7,922 | Strong learning |
| 57 | 0.065 | 7,461 | New best |
| 65 | 0.063 | 7,221 | Still improving |
| 80 | 0.065 | 7,492 | Convergence zone |

Best Genome (Gen 80, Post-Fix):
```
sensory_gain  = 5.10
binder_alpha  = 0.98   (heavy context binding)
weight_scale  = 0.40
ampa_jump     = 0.32
gaba_drop     = 11.54  (36:1 inhibition ratio!)
lr_scale      = 0.31   (strong plasticity)
thresh_offset = 0.0
tau_m         = 32.66  (slow membrane = temporal integration)
```

Key Discoveries:
- **Extreme E/I Imbalance**: `gaba/ampa ≈ 36:1`. The network uses massive inhibition to create sparse, input-discriminative firing patterns.
- **Near-unity binder_alpha** (0.98): The holographic binder almost fully overwrites context per token — acting as a direct token encoder rather than a sequence integrator.
- **High learning rate** (0.31): The MLP plasticity modulator is being driven hard, suggesting the discovered law produces small-magnitude updates that need amplification.
- **Slow membrane** (τ=33ms): Motor neurons integrate over ~3 ticks before firing, smoothing out spike noise.

7. Memory Budget (TPU v5e-1, 16 GB HBM)

| Component | Per Brain | × 32,768 | Total |
|---|---|---|---|
| W matrix (128×128×f32) | 64 KB | × 32,768 | 2.0 GB |
| Voltage + Traces (128×5×f32) | 2.5 KB | × 32,768 | 80 MB |
| All spikes buffer (10×128×f32) | 5 KB | × 32,768 | 160 MB |
| XLA Temp/Compilation | — | — | ~4 GB |
| **Total** | | | **~6.2 GB** |

Safe margin: 9.8 GB remaining for XLA optimizer and fusion buffers.

8. Production Inference Results (colab_tpu_nlp.py)

Architecture: Reservoir Computing
- SNN weights: FROZEN at blackbox genome values
- Readout: 256→2 linear map (128 linear + 128 quadratic features)
- Sensory: Gaussian population code (σ=0.08, 16 tuning curves per axis)
- Ticks: 30 per token

Convergence:
| Epoch | Accuracy | Output |
|---|---|---|
| 0 | 0/23 | (random) |
| 200 | 18/23 | HFLLL WMRLE THIS IS RRB |
| 600 | 20/23 | HELMO WORLD THIS IS RQB |
| 1000 | 22/23 | HELLO WORLD THIS IS RQA |
| 3800 | **23/23** | **HELLO WORLD THIS IS RRA** |

Bugs Resolved Along the Way:
1. Unit-circle motor decode (0.327 floor) → population rate coding
2. SNN weight saturation (online Hebbian) → reservoir computing (frozen weights)
3. Motor-only features (low diversity) → full 128-neuron readout
4. Thermometer encoding (1-neuron difference) → Gaussian population code
5. Online SGD interference → wider σ + more epochs

9. Next Steps
1. ✅ TPU blackbox discovered optimal genome (80 gens, loss=0.065)
2. ✅ Population rate coding broke 0.327 floor
3. ✅ Reservoir + linear readout achieved 23/23 perfect accuracy
4. Scale to longer/novel text sequences and measure generalization
5. Export R matrix + genome as `.safetensors` for C++ backend (`nn/brain_tree/`)
6. Evolve multiple reservoirs in parallel for multi-sentence processing

Report generated by Antigravity AI Engine.
